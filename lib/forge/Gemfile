source "https://rubygems.org"

# Most dependencies come from this gem
# gem 'forge-rad', '~> 3.2.13', :require => 'forge-rad'
gem 'forge-rad20', :require => 'forge-rad', :path => '/Users/adrianduyzer/www/forge-rad20'

# Extra Plugins
gem 'exception_notification', :require => 'exception_notifier'
gem 'rails-settings', :git => 'git://github.com/devlinzed/rails-settings.git'
gem 'launchy'
gem 'nokogiri', '1.5.10'

# The Groups
group :development, :test do
  gem 'faker'
  gem 'sqlite3'
end

group :test do
  gem 'capybara', '~> 2.0.0'
  gem 'rspec-rails', '>= 2.0.1'
  gem 'shoulda'
  gem 'shoulda-matchers', '~> 2.0.0'
  gem 'spork', '~> 0.9.0.rc'
  gem 'zentest-without-autotest', '4.4.0'
end

group :darwin do
  gem 'growl'
  gem 'rb-fsevent'
end
